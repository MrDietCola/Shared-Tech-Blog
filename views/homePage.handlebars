<div class="row">
  {{#each blogPostData as |blogPost| }}
  <div class=" card mb-4 project">
    <div class="col-md-5">
      <h2>
        {{blogPost.dataValues.title}}
      </h2>
      <p>
        {{blogPost.dataValues.description}}
      </p>
    </div>
  </div>
  {{/each}}
</div>
<script src="./js/logout.js"></script>
